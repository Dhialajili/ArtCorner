<form method="post" action="{{ path('app_comission_delete', {'Title': comission.Title}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comission.Title) }}">
    <button class="cancel">Supprimer</button>
</form>
